<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Contacts" Height="300" Width="520">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <!-- Left: Edit Contact -->
        <GroupBox Grid.Row="0" Grid.Column="0" Header="Edit Contact" Padding="10" Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Name:" Margin="3"/>
                <TextBox Grid.Row="1" Margin="3"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="2" Text="Phone Number:" Margin="3"/>
                <TextBox Grid.Row="3" Margin="3"
                         Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="4" Text="Email:" Margin="3"/>
                <TextBox Grid.Row="5" Margin="3"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </GroupBox>

        <!-- Right: Read Contact -->
        <GroupBox Grid.Row="0" Grid.Column="2" Header="Read Contact" Padding="10" Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Name:" Margin="3"/>
                <TextBlock Grid.Row="1" Margin="3" FontWeight="Bold"
                           Text="{Binding Name}"/>

                <TextBlock Grid.Row="2" Text="Phone Number:" Margin="3"/>
                <TextBlock Grid.Row="3" Margin="3" FontWeight="Bold"
                           Text="{Binding PhoneNumber}"/>

                <TextBlock Grid.Row="4" Text="Email:" Margin="3"/>
                <TextBlock Grid.Row="5" Margin="3" FontWeight="Bold"
                           Text="{Binding Email}"/>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" VerticalAlignment="Center">
            <Button Content="Save" Width="75" Height="25" Margin="3"
                    Command="{Binding SaveCommand}"/>
            <Button Content="Load" Width="75" Height="25" Margin="3"
                    Command="{Binding LoadCommand}"/>
        </StackPanel>
    </Grid>
</Window>